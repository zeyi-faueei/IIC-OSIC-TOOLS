* Set to 4 threads, if not changed otherwise
set num_threads=4
* assert BSIM compatibility mode with "nf" vs. "W"
set ngbehavior=hsa
* "nomodcheck" speeds up loading time
set ng_nomodcheck
* sky130A uses behaviour resistor in models, make sure they add noise
set enable_noisy_r
